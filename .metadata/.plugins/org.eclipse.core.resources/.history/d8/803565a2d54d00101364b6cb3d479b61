package in.ram.restcontroller;

import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.reactive.function.client.WebClient;

import in.ram.model.StockData;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;

@RestController
@RequestMapping("/api/stock/")
public class StockRestContoller {

	@GetMapping("/data/{companySymbol}/")
	public String fetDetails(@PathVariable String companySymbol) {
		WebClient client = WebClient.create();
		String token=""
		
		String url = "https://finnhub.io/api/v1/quote?symbol={companyName}&token={token}";

		StockData data = client.get().uri(url).retrieve().bodyToMono(StockData.class).block();
		System.out.println(data);
		return new String();
	}

}
